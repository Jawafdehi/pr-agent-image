# pr-agent-image

Builds `docker.io/jawafdehi/pr-agent` from a pinned The-PR-Agent/pr-agent commit (`--target github_app`).
Self-hosted CodeRabbit/Gemini replacement image. See `.github/workflows/build.yml`.
